version: "3"

services:
  llama-bot:
    image: aneeshjoy/llama-telegram-bot
    restart: always
    volumes:
      - /path/to/models:/models
    environment:
      BOT_TOKEN: <Add your telegram bot token here>
      MODEL_PATH: /models/ggml-alpaca-7b-q4.bin
      ALLOWED_USERS: ""
